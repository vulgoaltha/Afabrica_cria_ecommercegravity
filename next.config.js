/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['hjyihhevftudmkazvzcx.supabase.co'], // Add Supabase storage domain
    },
}

module.exports = nextConfig
